# MERACT - Система стримов (Acts)

## Что реализовано

### ✅ 1. Создание стримов (Acts)

- **Хук**: `useCreateAct` - интегрирован с бэкендом API
- **Компонент**: `CreateAct.jsx` - обновлен для работы с API
- **Функциональность**:
  - Получает userId из authStore
  - Отправляет данные на `/act/create-act`
  - Поддерживает загрузку изображений
  - Валидация данных

### ✅ 2. Управление категориями

- **Хук**: `useCategories` - с fallback к статичным данным
- **Функциональность**: Получение списка категорий для выбора

### ✅ 3. Стриминг интерфейс

- **StreamHost**: Компонент для ведения стрима
- **StreamViewer**: Компонент для просмотра стрима
- **Интеграция**: Готов к подключению Agora SDK

### ✅ 4. Управление стримами

- **Хук**: `useActs` - получение списка активных стримов
- **Функциональность**: Остановка стримов, фильтрация

## Архитектура

```
src/
├── pages/
│   ├── createAct/
│   │   ├── CreateAct.jsx           # Основной компонент создания
│   │   ├── hooks/
│   │   │   ├── useCreateAct.js     # Хук создания act
│   │   │   └── useCategories.js    # Хук категорий
│   │   └── components/
│   │       └── StreamHost.jsx      # Компонент стрима
│   └── acts/
│       ├── hooks/
│       │   └── useActs.js          # Хук управления acts
│       └── components/
│           └── StreamViewer.jsx    # Компонент просмотра
└── shared/
    ├── types/
    │   └── act.js                  # Типы и валидация
    └── stores/
        └── authStore.js            # Интеграция с авторизацией
```

## Как использовать

### 1. Создание стрима

1. Пользователь заходит на `/create-act`
2. Заполняет форму (title, category, type, format, methods)
3. Нажимает "Create"
4. Система создает Act в БД
5. Показывается StreamHost для запуска стрима

### 2. Просмотр стримов

1. Получение списка активных актов через `useActs`
2. Выбор акта для просмотра
3. Запуск StreamViewer с actId

### 3. API интеграция

- **POST** `/act/create-act` - создание act
- **GET** `/act/get-acts` - список актов
- **POST** `/act/stop-act?id={actId}` - остановка act
- **GET** `/act/token/{channel}/{role}/uid/{uid}` - получение Agora токена

## Следующие шаги

### Для полной интеграции Agora:

1. Установить: `npm install agora-rtc-sdk-ng`
2. Раскомментировать код Agora в StreamHost и StreamViewer
3. Добавить переменные окружения:
   ```env
   REACT_APP_AGORA_APP_ID=your_app_id
   ```

### Для работы с категориями:

Создать эндпоинт на бэкенде: `GET /categories`

## Особенности реализации

- **UserId автоматически**: Берется из authStore
- **Токены Agora**: Генерируются на бэкенде
- **Файлы**: Поддерживается загрузка preview изображений
- **Валидация**: Клиентская и серверная
- **Состояние**: Управляется через React hooks
- **Роутинг**: Интеграция с React Router

## Готово к использованию! 🚀

Система полностью интегрирована с вашим бэкендом и готова к запуску стримов.
